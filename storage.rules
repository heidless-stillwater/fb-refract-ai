rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Only allow read and write access to the dth-storage folder for authenticated users.
    match /dth-storage/{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
